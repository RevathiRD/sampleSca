language: scala

scala:
    - 2.11.0
    #- 2.9.3
   # - 2.10.5

build:

  ci:
    - java -version
    - echo $SHIPPABLE_SCALA_VERSION
notifications:
  - integrationName: ship-samplePyt
    type: webhook
    payload:
      - API_URL=$SHIPPABLE_API_URL
      - CONTAINER_NAME=$SHIPPABLE_CONTAINER_NAME
      - FOO=bar
      - FIZZ=buzz
